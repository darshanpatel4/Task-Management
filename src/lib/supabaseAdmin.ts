// This file is no longer needed as admin client usage is being handled directly
// in API routes or specific server actions where environment variables are guaranteed.
// Keeping it empty to prevent breaking existing imports, but it should be phased out.
export const supabaseAdmin = null;
