'use client';
// This is currently an alias for the admin project management page.
// It can be expanded to show project overviews for all users if needed.

import ProjectManagementPage from '../admin/projects/page';

export default function ProjectsPage() {
  return <ProjectManagementPage />;
}
